# Copy to .env.local and fill in your real GetMyShade API key.
# GMS_API_KEY must stay server-side — it is read only by app/api/gms/[...path]/route.ts.
GMS_API_KEY=sk_live_your_api_key
GMS_API_BASE=https://api.getmyshade.com/functions/v1
